<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>{%= title %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="{%= baseUrl %}/styles.css" />
  </head>

  <body>
    {%- include('header') %}
    <main>{%- content %}</main>

    {%- include('footer') %} {% if (options.content.scripts) { %} {%- options.content.scripts %} {% } %} {% if
    (livereload) { %}
    <script async defer src="http://localhost:35729/livereload.js"></script>
    {% } %}
  </body>
</html>
